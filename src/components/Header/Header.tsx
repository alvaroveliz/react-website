import React from 'react';

const Header: React.FC = (): React.ReactElement => {
    return (
        <header>
            <nav>
                <a>Home</a>
                <a>Blog</a>
                <a>Proyectos</a>
            </nav>
        </header>
    );
};

export default Header;
